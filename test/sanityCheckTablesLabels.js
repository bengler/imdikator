
const assert = require("chai").assert;

const dimensions = require('../data/dimensions.json');
const groups = require('../data/groups.json');

describe('Check groups', ()=> {
	describe('Lables', ()=> {
		it("matches all lables to groups and vice versa", ()=> {

			



		});
	});
});
